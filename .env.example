# --- Zora Coins MCP server configuration ---
# Get an API key at https://zora.co (Developer Settings)
ZORA_API_KEY=

# Base mainnet RPC endpoint (Alchemy/Infura/custom). Public RPC defaults if unset.
BASE_RPC_URL=https://mainnet.base.org

# Chain to use (writes are supported on Base mainnet; queries can work without this)
CHAIN_ID=8453

# Private key for onchain writes (0x-prefixed). Leave empty to run read-only.
PRIVATE_KEY=

# Optional: a platform referrer address to attribute referrals
PLATFORM_REFERRER=
